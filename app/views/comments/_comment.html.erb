<!-- Show comments -->

<% @article.comments.each do |c| %>
	<p>
		<%= c.content %>
	
  		<%= link_to 'Delete', article_comment_path(@article, c),
               method: :delete,
               data: { confirm: 'Are you sure?' } %>
	</p>
<% end %>